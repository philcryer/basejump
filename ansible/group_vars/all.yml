---
# gitconfig
gitconfig_name: philcryer
gitconfig_email: phil@philcryer.com

# directories
downloads: ~/.ansible/tmp/
code_directory: ~/repos

# packages
packages_installed:
  - fish
  - btop
  - curl
  - wget 
  - ncdu
  - pigz
  - unzip
  - clang
  - neovim
  - fontconfig

packages_installed_alpine:
  - gzip
  - libdeflate-utils

packages_installed_archlinux:
  - zlib-ng-compat
